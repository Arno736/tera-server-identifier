# Tera Server Identifier

## Usage

Show server name after guild name separate with `-` (Only in dungeons)

Mod can be enable/disable with `/8 si`

[GuildName - Server] or [Server] if no guild